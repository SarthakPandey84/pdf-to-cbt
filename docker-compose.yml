version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - POPPLER_PATH=/usr/bin

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend